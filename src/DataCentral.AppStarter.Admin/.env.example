# Leave empty to use Vite proxy in development
VITE_API_BASE_URL=

# Optional. Only for the one-file/frontend-only verification pattern.
# Recommended production pattern verifies on the backend instead.
VITE_WEBHOOK_HMAC_SECRET=

# Development only
VITE_TENANT_OVERRIDE=
